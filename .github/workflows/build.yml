name: build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  call-laravel:
    uses: DerManoMann/openapi-verifier/.github/workflows/laravel.yml@master
  call-slim:
    uses: DerManoMann/openapi-verifier/.github/workflows/slim.yml@master
